// GET /api/meta/stability -> {k_replay, agreement_status, agreement_artifacts, catatan}
// contracts.md Bagian 3. Angkanya adalah kesepakatan lintas-replay; tanpa satu
// pun replay yang pernah berjalan, setiap angka di sini akan karangan.

import { belumTersedia } from "@/lib/api";

export const GET = () => belumTersedia("Metrik stabilitas replay");
