// POST /api/replay/{inv_id} (mulai) — contracts.md Bagian 3.
// SSE replay memancarkan tiap event dari panggilan API LIVE; tidak ada respons
// kalengan (architecture.md). Karena itu M0 tidak membuka stream sama sekali.

import { belumTersedia } from "@/lib/api";

export const POST = () => belumTersedia("Replay orkestrasi agen");
